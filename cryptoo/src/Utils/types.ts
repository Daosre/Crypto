export type LoginProps = {
    email: string
    password: string
}

export type RegisterProps = {
    firstName: string
    lastName: string
    pseudo: string
    city: string
    email: string
    password: string
    promoCode: string
}